/*
La s funciones de tipo expresion son las que se asinan directamente a una varaible
tambien son llamadas funciones anonimas.
*/
// Declaración de una función de tipo expresión
let x = function (a, b) { return a + b};

resultado = x(1,2);
console.log( resultado )
